
export default class AuthService {
    constructor(opts) {}

    async userSignup(body, ns) {
        
        return {
            user: {
                user: 'test data'
            },
        };
    }
}
